version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DEBUG=false
      - LOG_LEVEL=INFO
    volumes:
      - ./vesper.db:/app/vesper.db
    restart: unless-stopped
